[tool.poetry]
name = "survey-parser-plugins"
version = "6.1.1a44"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "survey_parser_plugins"}]

[tool.poetry.dependencies]
python = ">=3.7,<3.11"
fastavro = "1.6.1"
python-snappy = "^0.6.1"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
pytest-cov = "^4.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

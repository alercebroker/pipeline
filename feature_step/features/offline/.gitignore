# DB credentials — contain a live password, never commit.
# Glob, not a list: write_credentials.json was added later and an exact
# name would have left it trackable.
*credentials.json

# Large regenerable query dumps (~11M) — regenerate with
# class_over_time/fetch_class_dates.py; the figures + README are committed.
class_over_time/class_dates_flat.csv
class_over_time/class_dates_top.csv

# Full oid dumps (~1 GB) — regenerate with scripts/offline_dump_oids.py
oids/

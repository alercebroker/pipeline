apiVersion: v1
kind: Service
metadata:
  name: {{ include "metadata_step.fullname" . }}
  labels:
    {{- include "metadata_step.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metadata_step.selectorLabels" . | nindent 4 }}

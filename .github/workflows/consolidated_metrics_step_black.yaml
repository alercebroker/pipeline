name: black_lint

on: 
  pull_request:
    paths:
      - 'consolidated_metrics_step/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
        with:
          options: "--check --verbose"
          src: "consolidated_metrics_step/"

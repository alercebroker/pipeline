DB_CONFIG = {
    "SQL": {
        "ENGINE": "postgresql",
        "HOST": "localhost",
        "USER": "postgres",
        "PASSWORD": "password",
        "PORT": 5432, # This gets changed to a random number inside test
        "DB_NAME": "test"
    }
}
